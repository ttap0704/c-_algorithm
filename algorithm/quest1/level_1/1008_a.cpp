#include <bits/stdc++.h>
using namespace std;

double A, B;

int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> A >> B;

  cout.precision(10);
  cout << (A / B) << '\n';
  return 0;
}