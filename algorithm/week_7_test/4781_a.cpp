#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll n, c, ret;
double m, p;



int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cout.precision(2);
  cout << fixed;

  while (true) {
    cin >> n >> m;
    if (n == 0 && m == 0.00) break;

    cout << ret << '\n';
  }

  return 0;
}