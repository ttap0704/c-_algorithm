#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll G, P, a[100004], sum1, sum2, ret;

int main () {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> G >> P;
  for (int i = 1; i <= P; i++) {
    cin >> a[i];
    
  }

  cout << ret << '\n';

  return 0;
}