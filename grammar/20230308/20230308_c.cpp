#include <bits/stdc++.h>
using namespace std;

// vector로 2차원 배열 만들기
vector<vector<int>> v;
vector<vector<int>> v2(10, vector<int>(10, 0));
vector<int> v3[10];

int main () {
  for(int i = 0; i < 10; i++){ 
    vector<int> temp; v.push_back(temp);
  }
  return 0;
}